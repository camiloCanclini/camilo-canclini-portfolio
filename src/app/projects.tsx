import { useThemeContext } from '@/providers/ThemeContext';
export default function ProjectSearcher() {

  const { theme } = useThemeContext();

  return (
    <div
        className="relative flex flex-col justify-center items-center gap-4 px-4 pl-32"
      >
        probando
      </div>
  );
}